import { View, Text } from 'react-native'
import React from 'react'
import { Trip } from '~/typings/trip'

const DayWisePlan = ({ trip }: { trip: Trip }) => {
    return (
        <View>
            <Text>DayWisePlan</Text>
        </View>
    )
}

export default DayWisePlan